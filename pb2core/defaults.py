DEFAULT_RUNTIME_SETTINGS = {
    "ingestion.keep_source_video": True,
    "ingestion.youtube.format": "bestvideo[ext=mp4]/best",
    "ingestion.youtube.rate_limit": None,
    "ingestion.max_concurrent_jobs": 2,
    "sampling.mode": "every_n_frames",
    "sampling.every_n_frames": 15,
    "sampling.target_fps": 2,
    "sampling.interval_seconds": 0.5,
    "sampling.image_format": "jpg",
    "sampling.jpeg_quality": 90,
    "sampling.max_frames_per_video": None,
    "model.bootstrap_weights": "yolov8n.pt",
    "model.ball_class_id": 32,
    "model.inference.conf_threshold": 0.25,
    "model.inference.iou_threshold": 0.45,
    "model.inference.imgsz": 640,
    "model.inference.device": "auto",
    "model.inference.half": False,
    "labeling.max_undo_steps": 20,
    "labeling.box_min_size_px": 4,
    "labeling.autosave": True,
    "ui.pagination_size": 24,
    "ui.poll_interval_ms": 2000,
    "training.base_weights": "yolov8n.pt",
    "training.epochs": 100,
    "training.imgsz": 640,
    "training.batch": 16,
    "training.val_split": 0.15,
    "training.device": "auto",
    "training.project_dir": "./data/datasets",
}
