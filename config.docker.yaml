storage:
  root: /data

database:
  url: sqlite:////data/db.sqlite3

server:
  host: 0.0.0.0
  port: 8000
  cors_origins: ["*"]
  # Host under a sub-path behind a reverse proxy, e.g. "/pb2-training".
  # Leave empty to serve at the domain root. Overridden by env PB2_BASE_PATH.
  base_path: ""
